name: Production Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up environment
        run: echo "Set up your prod environment"

      - name: Deploy to Production
        run: |
          echo "Run your deployment script here"
